import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const background = Color(0xFF050510);
  static const surface = Color(0xFF111111);
  static const border = Color(0xFF1E1E1E);

  static const text = Color(0xFFFFFFFF);
  static const muted = Color(0xFF555555);

  // Accent padrão — pode ser trocado por tema do usuário
  static const accent = Color(0xFF00FF87);
  static const secondary = Color(0xFFFF6B35);

  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFEAB308);
  static const success = Color(0xFF22C55E);
}
